import hero from './hero';
import adminMessage from './adminMessage';
import section from './section';
import gallery from './gallery';

export const schemaTypes = [hero, adminMessage, section, gallery];
